<nav>
  <%= site_header(user_signed_in? ? current_user.site : "Top Stories") %>
  <% if user_signed_in? %>
    <%= link_to raw('Logout <i class="fa fa-sign-out"></i>'), user_logout_path, class: 'logout' %>
  <% end %>
</nav>
